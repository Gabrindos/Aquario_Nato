import { StyleSheet } from "react-native"

export const colors = {
    white : '#FFF',
    primary: '050a30'
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})